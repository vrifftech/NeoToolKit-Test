cmake_minimum_required(VERSION 3.24)
cmake_policy(SET CMP0091 NEW)
project(NeoToolKitTest VERSION 1.0.0 LANGUAGES CXX)
if(WIN32)
    enable_language(RC)
endif()
include(GNUInstallDirs)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
if(NOT DEFINED CMAKE_MSVC_RUNTIME_LIBRARY)
    set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>" CACHE STRING "Shared host/module runtime")
endif()
option(NEOTOOLKIT_BUILD_GUI "Build the actual native workspace application" ON)
option(NEOTOOLKIT_BUILD_STANDALONES "Also build all nine standalone tool wrappers" OFF)
option(NEOTOOLKIT_WARNINGS_AS_ERRORS "Treat host warnings as errors" OFF)
if(EMSCRIPTEN)
    message(FATAL_ERROR "NeoToolKit-Test 1.0.0 is a native hosting milestone. The standalone tools retain their browser builds; this host is not yet browser-qualified.")
endif()
set(NEO_HOSTED_BUILD ON)
set(NEOSHARED_ENABLE_ERF ON CACHE BOOL "Archive core required by game explorer" FORCE)
include(cmake/Components.cmake)
neo_component(NEOSHARED neoshared "https://github.com/vrifftech/neoshared.git" "wx/NeoModulePanel.hpp")
file(READ "${NEOTOOLKIT_NEOSHARED_SOURCE_DIR}/wx/NeoModulePanel.hpp" _panel_api)
if(NOT _panel_api MATCHES "validateOutput")
    message(FATAL_ERROR "Update NeoShared: this host requires the output-guard/snapshot module API revision")
endif()
if(NOT EXISTS "${NEOTOOLKIT_NEOSHARED_SOURCE_DIR}/include/neoshared/GffResourceDocument.hpp")
    message(FATAL_ERROR "Update NeoShared: NeoToolKit-Test 1.0.0 requires the shared GFF resource-snapshot adapter")
endif()
set(NEOSHARED_ROOT "${NEOTOOLKIT_NEOSHARED_SOURCE_DIR}" CACHE PATH "Matched shared source" FORCE)
add_subdirectory("${NEOSHARED_ROOT}" "components/neoshared" EXCLUDE_FROM_ALL)
foreach(component IN ITEMS NEOBIF NEO2DA NEOSSF NEOTLK NEOGFF NEODLG NEOJRL NEOTPC NEOERF)
    set(${component}_BUILD_CLI OFF CACHE BOOL "The workspace uses module libraries, not command-line frontends" FORCE)
    set(${component}_BUILD_WX_GUI ${NEOTOOLKIT_BUILD_STANDALONES} CACHE BOOL "Standalone wrapper" FORCE)
    set(${component}_BUILD_WX_MODULE ${NEOTOOLKIT_BUILD_GUI} CACHE BOOL "Embedded panel" FORCE)
    set(${component}_REQUIRE_WX_GUI ${NEOTOOLKIT_BUILD_GUI} CACHE BOOL "Never silently omit the product" FORCE)
endforeach()
neo_component(NEOTPC NeoTPC "https://github.com/vrifftech/NeoTPC.git" "src/wx/TextureEditorPanel.hpp")
add_subdirectory("${NEOTOOLKIT_NEOTPC_SOURCE_DIR}" "components/NeoTPC")
neo_component(NEOERF NeoERF "https://github.com/vrifftech/NeoERF.git" "src/wx/ERFEditorPanel.hpp")
add_subdirectory("${NEOTOOLKIT_NEOERF_SOURCE_DIR}" "components/NeoERF")
neo_component(NEOBIF NeoBIF "https://github.com/vrifftech/NeoBIF.git" "src/wx/BrowserPanel.hpp")
neo_component(NEO2DA Neo2DA "https://github.com/vrifftech/Neo2DA.git" "src/wx/EditorPanel.hpp")
neo_component(NEOSSF NeoSSF "https://github.com/vrifftech/NeoSSF.git" "src/wx/SSFEditorPanel.hpp")
neo_component(NEOTLK NeoTLK "https://github.com/vrifftech/NeoTLK.git" "src/wx/TLKEditorPanel.hpp")
neo_component(NEOGFF NeoGFF "https://github.com/vrifftech/NeoGFF.git" "src/wx/GFFEditorPanel.hpp")
add_subdirectory("${NEOTOOLKIT_NEOGFF_SOURCE_DIR}" "components/NeoGFF")
neo_component(NEODLG NeoDLG "https://github.com/vrifftech/NeoDLG.git" "src/wx/DLGEditorPanel.hpp")
add_subdirectory("${NEOTOOLKIT_NEODLG_SOURCE_DIR}" "components/NeoDLG")
neo_component(NEOJRL NeoJRL "https://github.com/vrifftech/NeoJRL.git" "src/wx/JRLEditorPanel.hpp")
add_subdirectory("${NEOTOOLKIT_NEOJRL_SOURCE_DIR}" "components/NeoJRL")
add_subdirectory("${NEOTOOLKIT_NEOSSF_SOURCE_DIR}" "components/NeoSSF")
add_subdirectory("${NEOTOOLKIT_NEOTLK_SOURCE_DIR}" "components/NeoTLK")
add_subdirectory("${NEOTOOLKIT_NEOBIF_SOURCE_DIR}" "components/NeoBIF")
add_subdirectory("${NEOTOOLKIT_NEO2DA_SOURCE_DIR}" "components/Neo2DA")
function(neo_host_warnings target)
    if(MSVC)
        target_compile_options(${target} PRIVATE /W4 /permissive-)
        if(NEOTOOLKIT_WARNINGS_AS_ERRORS)
            target_compile_options(${target} PRIVATE /WX)
        endif()
    else()
        target_compile_options(${target} PRIVATE -Wall -Wextra -Wpedantic)
        if(NEOTOOLKIT_WARNINGS_AS_ERRORS)
            target_compile_options(${target} PRIVATE -Werror)
        endif()
    endif()
endfunction()
if(NEOTOOLKIT_BUILD_GUI)
    if(NOT TARGET neobif::wx OR NOT TARGET neo2da::wx OR NOT TARGET neossf::wx OR NOT TARGET neotlk::wx OR NOT TARGET neogff::wx OR NOT TARGET neodlg::wx OR NOT TARGET neojrl::wx OR NOT TARGET neotpc::wx OR NOT TARGET neoerf::wx)
        message(FATAL_ERROR "All nine matched reusable editor modules are required")
    endif()
    add_library(neotoolkit_workspace STATIC src/WorkspaceFrame.cpp)
    target_include_directories(neotoolkit_workspace PUBLIC src)
    target_link_libraries(neotoolkit_workspace PUBLIC neobif::wx neo2da::wx neossf::wx neotlk::wx neogff::wx neodlg::wx neojrl::wx neotpc::wx neoerf::wx)
    neo_host_warnings(neotoolkit_workspace)
    if(WIN32)
        add_executable(NeoToolKit-Test WIN32 src/main.cpp)
    elseif(APPLE)
        add_executable(NeoToolKit-Test MACOSX_BUNDLE src/main.cpp)
        set_target_properties(NeoToolKit-Test PROPERTIES
            MACOSX_BUNDLE_BUNDLE_NAME "NeoToolKit Test"
            MACOSX_BUNDLE_GUI_IDENTIFIER "com.vrifftech.neotoolkit-test"
            MACOSX_BUNDLE_SHORT_VERSION_STRING "${PROJECT_VERSION}")
    else()
        add_executable(NeoToolKit-Test src/main.cpp)
    endif()
    target_link_libraries(NeoToolKit-Test PRIVATE neotoolkit_workspace)
    neo_host_warnings(NeoToolKit-Test)
    target_compile_definitions(neotoolkit_workspace PUBLIC NEOTOOLKIT_VERSION="${PROJECT_VERSION}")
    include(cmake/Packaging.cmake)
    neotoolkit_configure_packaging()
endif()
